<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8">
    <meta http-equiv="cache-control" content="max-age=0">
    <meta http-equiv="cache-control" content="no-cache">
    <meta http-equiv="expires" content="0">
    <meta http-equiv="expires" content="Tue, 01 Jan 1980 1:00:00 GMT">
    <meta http-equiv="pragma" content="no-cache">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{ tinyib.main.desc }}</title>
    <link rel="shortcut icon" href="favicon.ico">
		<link rel="stylesheet" type="text/css" href="css/global.css">
		<link rel="stylesheet" type="text/css" href="loadstyle.php">
    <link rel="stylesheet" type="text/css" href="css/index.css?v=4">
    {% for style in tinyib.main.alternatestyles %}
    <link rel="alternate stylesheet" type="text/css" href="css/{{ style.css }}.css" title="{{ style.name }}">
    {% endfor %}
    <script src="js/jquery.js"></script>
    <script src="js/tinyib.js"></script>
  </head>
  <body>
    <div class="adminbar">
      <select id="switchStylesheet">
        <option value="">Style</option>
        <option value="{{ tinyib.main.defaultstyle.css }}">{{ tinyib.main.defaultstyle.name }}</option>
        {% for style in tinyib.main.alternatestyles %}
        <option value="{{ style.css }}">{{ style.name }}</option>
        {% endfor %}
      </select>
    </div>
    <div class="logo">
      <div class="logo-container">
        <img src="logo.png" alt="Logo" width="132" height="128" style="vertical-align:middle;"><span>{{ tinyib.main.title }}</span>
      </div>
    </div>
    <hr width="90%">
    <div class="content">
    <div class="boards">
      <h4>Boards</h4>
      <div class="boards-grid">
        {% for category in tinyib.main.categories %}
          <div class="boards-cell">
            <strong>{{ category.name }}</strong><br>
            {% for board in category.boards %}
              {% if 'url' not in board %}
                <a href="/{{ board.key }}/">{{ board.name }}</a><br>
              {% else %}
                <a href="{{ board.url }}" target="_blank">{{ board.name }}</a><br>
              {% endif %}
            {% endfor %}
          </div>
        {% endfor %}
      </div>
    </div>
    <hr>
    <div class="siterules">
      {{ tinyib.main.rules }}
      <a href="privacy.txt">Privacy Policy</a>
    </div>
    </div>
    <hr>
    <div class="footer">
      -
      <a href="http://www.2chan.net" target="_top">futaba</a>
      +
      <a href="http://www.1chan.net" target="_top">futallaby</a>
      +
      <a href="https://code.rocketnine.space/tslocum/tinyib" target="_top">tinyib</a>
      -
    </div>
  </body>
</html>